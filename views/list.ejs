<!DOCTYPE html>
<html lang="en">
<head>
    <title>projects</title>
    <link rel="stylesheet" href="css/style.css">
    meta charset="UTF-8">
</head>
<body>
<div class="box">
    <h1>ToDo list</h1>
</div>
<div class="box">
    <% dayej.forEach(function(items){ %>
    <form action="/delete" method=""POST>
        <div class="item">
            <input type="checkbox" name="checkbox" value="<%= items._id %>" onChange="this.form.submit()">
            <p><%= items.name%></p>
        </div>
    </form>
    <% for(var i =0;i< dayej.length;i++){ %>
        <div class="item">
            <input type="checkbox" name="checkbox1" id="">
            <p><%= dayej[i].name%></p>
        </div>
    <% } %>
    <form  class="item" action="/" method=""POST">  
    <input type="text" name="ele1" placeholder="enter item">
    <button type="submit">+</button>
    </form>
</div>
</body>
</html>